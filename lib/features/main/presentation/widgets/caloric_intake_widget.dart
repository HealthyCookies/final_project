import 'package:flutter/material.dart';

class CaloricIntakeWidget extends StatelessWidget {
  const CaloricIntakeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Caloric Intake Widget',
      ),
    );
  }
}
